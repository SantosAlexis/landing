// main.js
document.addEventListener("DOMContentLoaded", () => {
    // Aquí podemos agregar cualquier lógica adicional si es necesario.
    // Aplica la animación cuando se carga la página

    function ira3() {
        window.location.hash = "#selfEvalSection";
        // O llamar a tu función de navegación para activar esa sección
        navigation.showSection("selfEval");
    }
});
